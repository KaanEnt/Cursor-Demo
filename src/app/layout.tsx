import type { Metadata } from 'next'
import { Inter } from 'next/font/google'
import './globals.css'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'DocuMind AI - AI-Powered Document Intelligence',
  description: 'Transform your document processing with AI. Extract, analyze, and understand documents at scale.',
  keywords: 'AI document processing, document intelligence, contract analysis, legal tech, business automation',
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>
        {children}
      </body>
    </html>
  )
} 